# 项目面试准备手册

> 这份文档帮你快速理解项目架构、每个文件干什么、以及面试时怎么讲。



***

## 一、整体架构思路

### 为什么要分层？

如果所有测试代码写在一个文件里：接口请求、断言、测试数据全混在一起，改一个字段要改三处，换个环境要改一堆 URL。所以我按**职责**拆成了五层：



```
testcases/   →  只写"测什么"（测试用例，描述测试意图）

api/         →  只写"怎么调"（接口封装，发HTTP请求）

utils/       →  只写"公共能力"（日志、断言、数据加载）

config/      →  只写"配置"（URL、超时、默认参数）

testdata/    →  只写"数据"（城市列表、参数化数据）
```

**核心原则：改业务不动框架，改数据不动代码。**

比如要加一个新城市测试，只改 `testdata/cities.yaml`；要换个测试环境，只改 `.env`；要加新接口，只在 `api/` 加一个类。

### 请求流程



```
测试用例

&#x20; → 调用 api/xxx\_api.py 的方法（如 get\_forecast）

&#x20;   → 继承 base\_api.py 的 get() 方法（统一发请求、记日志、设超时）

&#x20;     → requests 发 HTTP 请求

&#x20;   ← 返回 response

&#x20; → 调用 utils/assert\_util.py 的断言方法（校验状态码、字段、类型）

&#x20; ← 测试通过/失败
```



***

## 二、每个文件干什么

### config/ 目录（配置层）

`config.py`



* 作用：项目的 "总开关"，所有配置都从这里读

* 原理：用 python-dotenv 加载 `.env` 文件，把环境变量读进来

* 为什么这么做：URL、超时这些是会变的，不能硬编码在代码里。换开发 / 测试 / 生产环境只改 `.env` 不改代码

* 关键函数：


  * `load_dotenv()` — 读取 `.env` 文件

  * `os.getenv()` — 从环境变量取值，带默认值

`.env`**&#x20;/&#x20;**`.env.example`



* 作用：存敏感配置和环境差异

* `.env.example` 是模板，提交到 Git；`.env` 是真实配置，不提交（防止泄露）



***

### api/ 目录（接口层）

`base_api.py`**&#x20;— 请求基类**



* 作用：所有接口类的 "爸爸"，封装了所有请求的公共逻辑

* 为什么需要它：如果每个接口类都自己写 `requests.get()`，那超时设置、日志记录、session 复用就得写 N 遍。基类写一次，子类继承就行

* 关键设计：


  * `__init__(self, base_url)` — 每个子类传自己的 host，比如天气预报用 `api.open-meteo.com`，空气质量用 `air-quality-api.open-meteo.com`

  * `self.session = requests.Session()` — 用 Session 复用 TCP 连接，比每次新建请求快

  * `get(path, params)` — 统一 GET 方法，自动记日志（URL、参数、响应码、耗时）

`forecast_api.py`**&#x20;— 天气预报接口**



* 作用：封装天气预报相关的所有调用方法

* 为什么单独一个文件：天气预报有 current/hourly/daily 三种模式，参数多，独立成类

* 方法：`get_forecast(lat, lon, current=..., hourly=..., daily=...)` — 调用者传需要的参数就行，不用拼 URL

`geocoding_api.py`**&#x20;— 城市搜索接口**



* 作用：根据城市名查经纬度和 LocationID

* 方法：`search(name, count, language)`

`air_api.py`**&#x20;— 空气质量接口**



* 作用：查 PM2.5、PM10、AQI

* 为什么单独一个 host：空气质量数据在 Open-Meteo 的另一个子域名上



***

### utils/ 目录（工具层）

`logger.py`**&#x20;— 日志**



* 作用：所有模块共用一个日志器，同时输出到控制台和文件

* 为什么需要：测试跑起来几百个请求，出问题得能追溯。文件日志存到 `reports/logs/`，按时间命名

* 原理：Python 标准库 logging，加了两个 Handler（控制台 StreamHandler + 文件 FileHandler）

`assert_util.py`**&#x20;— 断言工具**



* 作用：把常用断言封装成有意义的方法名

* 为什么要封装：测试用例里写 `assert resp.status_code == 200 and "current" in resp.json()` 太长，而且失败时不知道哪步错了。封装成 `assert_http_status(resp, 200)` + `assert_has_key(data, "current")`，失败信息更清晰

* 核心方法：


  * `assert_http_status(resp, 200)` — 校验 HTTP 状态码

  * `assert_no_error(resp)` — 校验响应体里没有 error 字段

  * `assert_has_key(data, "current")` — 校验字段存在

  * `assert_range(value, -90, 60, "温度")` — 校验数值在合理范围

  * `assert_nonempty(list, "结果")` — 校验集合非空

`data_loader.py`**&#x20;— YAML 数据加载**



* 作用：从 `testdata/cities.yaml` 读城市数据

* 为什么用 YAML 不用 Python 列表：测试数据是非技术人员也能看的格式，改数据不用改代码



***

### testcases/ 目录（测试用例层）

`conftest.py`**&#x20;— pytest 的 "公共仓库"**



* 作用：定义 fixture（pytest 的依赖注入机制）

* 原理：fixture 是 `@pytest.fixture` 标记的函数，测试用例只要在参数里写上它的名字，pytest 就会自动把结果传进来

* 我们定义了这些 fixture：


  * `forecast_api` — 整个测试会话共用一个天气预报 API 实例（scope="session"）

  * `geocoding_api` — 城市搜索 API 实例

  * `air_api` — 空气质量 API 实例

  * `cities` — 从 YAML 加载的城市列表

  * `default_location` — 默认测试坐标（广州）

`test_forecast.py`**&#x20;— 天气预报测试**



* 分了几个测试类：


  * `TestCurrentWeather` — 实时天气（多城市、温度单位切换）

  * `TestDailyForecast` — 每日预报（3 天、7 天、天数边界）

  * `TestHourlyForecast` — 逐小时预报

  * `TestBoundary` — 边界值（南北极、经纬度极值）

  * `TestNegative` — 异常场景（非法变量名、错误参数类型）

`test_geocoding.py`**&#x20;— 城市搜索测试**



* `TestCitySearch` — 中文搜索、英文搜索、多城市参数化

* `TestBoundary` — 返回数量边界

* `TestNegative` — 不存在的城市、空查询

`test_air.py`**&#x20;— 空气质量测试**



* `TestAirNow` — 实时空气质量、多城市

* `TestAirHourly` — 逐小时预报

* `TestAirNegative` — 非法坐标



***

### testdata/ 目录（数据层）

`cities.yaml`



* 作用：存城市参数化测试数据

* 用法：测试用例用 `@pytest.mark.parametrize` 或直接从 fixture 读，不用在代码里硬编码城市列表



***

### .github/workflows/ci.yml — CI/CD



* 作用：每次 push 代码或 PR 时，GitHub 自动跑一遍测试

* 原理：GitHub Actions 提供 Ubuntu 虚拟机，自动装 Python、装依赖、跑 pytest、上传报告



***

## 三、测试过程中踩过的 Bug（面试可讲）

### Bug 1：Geo 接口返回 404



* **现象**：城市搜索接口一直 404，body 是空的

* **排查**：一开始以为路径错了，试了 `/geo/v2/city/lookup` 和 `/v2/city/lookup` 都 404

* **根因**：和风天气 2026 年把 Geo 接口从公共域名 `geoapi.qweather.com` 迁移到了用户专属域名

* **解决**：Geo 接口也改用专属 host，和天气接口同一个域名

* **面试官问怎么查的**：看 HTTP 状态码是 404 不是 401 → 说明域名 / 路径不对，不是鉴权问题 → 查官方文档发现新版 Geo 接口走专属 host

### Bug 2：401 鉴权失败



* **现象**：所有天气接口都返回 401 "Authentication failed"

* **排查**：先试了 query 传 key 和 header 传 key 两种方式，都 401 → 说明不是传参方式问题 → 看控制台发现可用额度 0

* **根因**：和风天气改为按量计费模式，新用户需要在项目里订阅 API 产品才能调用

* **解决**：切换到 Open-Meteo（免费、无需注册）

### Bug 3：空响应体 JSON 解析崩溃



* **现象**：404 接口返回空 body，调 `resp.json()` 直接抛异常 `JSONDecodeError`

* **根因**：不是所有响应都是 JSON，404 页面可能是 HTML 或空字符串

* **解决**：在断言工具里加了 `_safe_json()` 方法，try-except 包裹，解析失败返回 None 而不是崩掉

### Bug 4：错误响应结构不兼容



* **现象**：和风天气新版错误格式是 `{"error":{"status":401}}`，老版是 `{"code":"404"}`，断言写的是读 `code` 字段

* **解决**：封装了统一的错误断言，先判断有没有 `error` 字段（新版），没有再读 `code` 字段（老版）

### Bug 5：空气质量接口 404



* **现象**：Open-Meteo 空气质量接口返回 404

* **根因**：空气质量数据在独立的子域名 `air-quality-api.open-meteo.com`，不是主域名

* **解决**：在 config 里加了 `AIR_BASE_URL`，AirQualityApi 用独立 host



***

## 四、面试项目介绍（背这段）

> 这是我独立设计的一个天气数据中心接口自动化测试框架，基于 pytest+requests，覆盖天气预报、城市搜索、空气质量三大业务模块，共 42 个测试用例。
> 框架采用分层架构：配置层管理多环境地址，接口层按业务域封装请求，工具层封装了统一的日志和断言，测试用例层只关注测试逻辑。数据驱动方面，城市测试数据存在 YAML 文件里，改数据不用改代码。
> 测试类型上覆盖了正向用例、参数化测试、边界值测试和异常场景测试。CI 方面配了 GitHub Actions，每次 push 自动跑测试并生成 HTML 报告。
> 开发过程中踩过几个坑：比如不同版本 API 的错误响应结构不兼容、某些服务迁移了域名导致 404，我都通过封装统一的错误处理和域名配置解决了。



***

## 五、面试官可能问的问题 & 回答

**Q：为什么用 pytest 不用 unittest？**

A：pytest 的 fixture 机制比 unittest 的 setUp/tearDown 灵活，支持参数化装饰器，断言直接用 assert 不用 self.assertEqual，写起来更简洁。

**Q：fixture 的 scope="session" 是什么意思？**

A：表示这个 fixture 在整个测试会话只执行一次，所有测试用例共享同一个 API 实例，不用每个用例都新建连接。

**Q：数据驱动怎么做的？**

A：城市数据存在 testdata/cities.yaml 里，通过 data\_loader 加载成 Python 列表，测试用例用 @pytest.mark.parametrize 参数化，一个城市跑一遍。

**Q：如果要加一个新接口测试怎么做？**

A：三步：1）在 api / 下加一个类继承 BaseApi；2）在 conftest.py 里加对应的 fixture；3）在 testcases / 下加测试文件。不用动任何公共代码。

**Q：怎么做断言的？**

A：封装了 assert\_util 工具类，把 HTTP 状态码校验、字段存在性、数值范围这些常用断言封装成有语义的方法，失败时打印具体哪一步错了，不用自己写一堆 assert。